{
  "name": "asw-docs",
  "version": "0.1.0",
  "description": "Documentation for ASW, a C++ wrapper for SDL3 for making games.",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.16",
    "vitepress-plugin-llms": "^1.11.1",
    "vue": "^3.5.28"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "mcp:build": "cd mcp && yarn install && yarn build",
    "mcp:start": "node mcp/dist/index.js"
  }
}
